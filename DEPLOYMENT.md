# Инструкция по деплою NEO Store на GitHub

## Подготовка проекта

Проект уже подготовлен к деплою:
- ✅ Git репозиторий инициализирован
- ✅ .gitignore настроен
- ✅ .env.example создан
- ✅ README.md обновлен

## Шаги для загрузки на GitHub

### 1. Создайте репозиторий на GitHub

1. Перейдите на [GitHub.com](https://github.com)
2. Нажмите кнопку "New" или "+" → "New repository"
3. Введите название репозитория (например: `NEO_Store`)
4. Выберите Public или Private
5. **НЕ** создавайте README, .gitignore или лицензию (они уже есть)
6. Нажмите "Create repository"

### 2. Подключите локальный репозиторий к GitHub

После создания репозитория GitHub покажет инструкции. Выполните следующие команды:

```bash
# Добавьте удаленный репозиторий (замените YOUR_USERNAME и REPO_NAME)
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Загрузите код на GitHub
git push -u origin main
```

### 3. Альтернативный способ (если используете SSH)

```bash
git remote add origin git@github.com:YOUR_USERNAME/REPO_NAME.git
git branch -M main
git push -u origin main
```

## Важные замечания

### ⚠️ Безопасность

- **НЕ** загружайте файл `.env` в репозиторий
- **НЕ** загружайте `db.sqlite3` (база данных)
- **НЕ** загружайте папку `/media` (пользовательские загрузки)
- **НЕ** загружайте папку `/staticfiles` (собранные статические файлы)

Все эти файлы уже добавлены в `.gitignore`.

### 📝 Переменные окружения

Для продакшена создайте файл `.env` на сервере с следующими переменными:

```env
SECRET_KEY=your-production-secret-key
DEBUG=False
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com
```

### 🔄 Обновление кода

После внесения изменений:

```bash
# Добавьте изменения
git add .

# Создайте коммит
git commit -m "Описание изменений"

# Загрузите на GitHub
git push
```

## Деплой на продакшен

Подробные инструкции по деплою на различные платформы находятся в папке `setup guides/`:
- Railway: `setup guides/host_on_railway.md`
- AWS S3 для медиа-файлов: `setup guides/host_static_media_files_s3.md`
- База данных на Railway: `setup guides/provision_database_on_railway.md`

## Полезные команды Git

```bash
# Проверить статус
git status

# Посмотреть историю коммитов
git log

# Создать новую ветку
git checkout -b feature-name

# Переключиться на ветку
git checkout branch-name

# Объединить изменения
git merge branch-name
```

## Поддержка

Если возникли проблемы:
1. Проверьте, что все файлы добавлены: `git status`
2. Убедитесь, что `.env` не в репозитории: `git ls-files | grep .env`
3. Проверьте `.gitignore`: `cat .gitignore`

---

**Успешного деплоя! 🚀**

