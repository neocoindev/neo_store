# Руководство по управлению баннерами через админку

## ✅ Что было сделано

1. ✅ Создана модель `Banner` в `store/models.py`
2. ✅ Добавлен `BannerAdmin` в `store/admin.py`
3. ✅ Обновлен view `index` для передачи баннеров в шаблон
4. ✅ Обновлен шаблон `templates/store/index.html` для использования динамических баннеров
5. ✅ Создана миграция `store/migrations/0020_banner.py`

## 📋 Следующие шаги

### 1. Применить миграцию

Выполните в терминале:
```bash
python manage.py migrate store
```

### 2. Войти в админку Django

1. Откройте `/admin/` в браузере
2. Войдите как суперпользователь
3. Найдите раздел **"Баннеры"** (Store → Баннеры)

## 🎨 Как создать баннеры

### Главный баннер (большой)

1. Нажмите **"Добавить баннер"**
2. Заполните поля:
   - **Тип баннера**: выберите "Главный баннер"
   - **Заголовок**: например, "Лучшие предложения недели"
   - **Текст**: описание (опционально)
   - **Текст кнопки**: например, "Смотреть все товары"
   - **Ссылка кнопки**: 
     - Относительная: `/shop/` или `/category/1/`
     - Абсолютная: `https://example.com/shop/`
     - Django URL: можно использовать, но лучше относительные пути
   - **Изображение**: загрузите изображение (опционально)
   - **Цвет фона или градиент**: 
     - Простой цвет: `#FF0000`
     - Градиент: `linear-gradient(135deg, #FF0000 0%, #ff6b35 100%)`
   - **Статус**: выберите "Published" для отображения
   - **Порядок сортировки**: чем меньше число, тем выше баннер (0 = самый верхний)

3. Нажмите **"Сохранить"**

### Малые баннеры (3 штуки)

Создайте 3 баннера с типом **"Малый баннер"**:

**Баннер 1:**
- Тип: Малый баннер
- Заголовок: "Быстрая доставка"
- Текст: "Доставка по всему Кыргызстану"
- Цвет фона: `linear-gradient(135deg, #4caf50 0%, #66bb6a 100%)`
- Порядок: 0

**Баннер 2:**
- Тип: Малый баннер
- Заголовок: "Гарантия качества"
- Текст: "Официальная гарантия на все товары"
- Цвет фона: `linear-gradient(135deg, #667eea 0%, #764ba2 100%)`
- Порядок: 1

**Баннер 3:**
- Тип: Малый баннер
- Заголовок: "Бонусы"
- Текст: "Кэшбэк и специальные предложения"
- Цвет фона: `linear-gradient(135deg, #f093fb 0%, #f5576c 100%)`
- Порядок: 2

## 🎯 Примеры цветов и градиентов

### Простые цвета:
- Красный: `#FF0000`
- Зеленый: `#4caf50`
- Синий: `#2196F3`
- Фиолетовый: `#9c27b0`

### Градиенты:
- Красный градиент: `linear-gradient(135deg, #FF0000 0%, #ff6b35 100%)`
- Зеленый градиент: `linear-gradient(135deg, #4caf50 0%, #66bb6a 100%)`
- Синий градиент: `linear-gradient(135deg, #667eea 0%, #764ba2 100%)`
- Розовый градиент: `linear-gradient(135deg, #f093fb 0%, #f5576c 100%)`

## 📝 Важные замечания

1. **Главный баннер**: Отображается только первый баннер с типом "main" и статусом "Published"
2. **Малые баннеры**: Отображаются первые 3 баннера с типом "small" и статусом "Published"
3. **Порядок сортировки**: Используйте поле "Порядок сортировки" для управления последовательностью
4. **Изображения**: Если загружено изображение, оно будет использовано вместо цвета фона
5. **Fallback**: Если баннеры не созданы, отображаются дефолтные баннеры

## 🔧 Технические детали

- Модель: `store.models.Banner`
- Админка: `/admin/store/banner/`
- View: `store.views.index`
- Шаблон: `templates/store/index.html`

## 🚀 Готово!

После создания баннеров они автоматически появятся на главной странице сайта.

